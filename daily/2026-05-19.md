# 2026-05-19 学习笔记（Cohort Day 2 · 对话沉淀型）

> 本笔记是"对话沉淀型"：晚上通过和 Claude Code 一问一答把不懂的点搞清楚，沉淀进笔记。
>
> **重要时间线修正**：之前我自己脑补的 "Day 3" 标签是错的。WCB API 拉到 program 起始日 = **2026-05-18**，所以：
> - 2026-05-17 = 开营仪式（pre-cohort，周日晚）
> - 2026-05-18 = **Cohort Day 1**
> - 2026-05-19（今天） = **Cohort Day 2**
> - Cohort 总时长 4 周 → 06-14 结束

## 今日主题
**智能合约 / EOA / EVM 三层模型 + 合约账户的真实价值**

主题调整说明：原计划"签名分类深挖"被砍掉。原因是发现 Handbook web3 板块里 smart-contract / dev-stack / cryptography 都没正式读过，签名属于偏后的应用题。今晚先把"链上账户 + 执行环境"的基础模型搞清楚，签名/钓鱼留到走到 security 章节再做。

**WCB 任务对齐（晚上接入 API 后才看清的真实进度）**：今晚 4 轮 Q&A 实际**直接完成了 WCB Week 1 的 30 分进阶任务 #14**「比较 EOA、智能账户、多签的权限差异」+ 大半个 10 分任务 #11「Web3 基础概念卡片」。误打误撞但路径吻合。详见末尾"WCB 任务命中清单"。

参考 Handbook：
- 主：[web3/smart-contract](https://aiweb3.school/zh/handbook/web3/smart-contract/)
- 关联：[web3/wallet](https://aiweb3.school/zh/handbook/web3/wallet/)（已读）、[web3/account-abstraction](https://aiweb3.school/zh/handbook/web3/account-abstraction/)（涉及但未深入）

## 关键收获（晚上 Q&A 沉淀）

### 1. 智能合约 = 部署在链上的程序，不是"自动执行的合同"

"Smart Contract" 是营销词，**它不智能也不是合同**。

本质：跟 0518 那笔 ETH 转账走完全同一条管道（钱包签名 → RPC 广播 → 节点执行 → 改状态 → 上链 → explorer 可查），只是节点**多跑一步**：执行合约里部署好的 EVM 字节码。

跟普通后端代码的关键差异：

| 普通后端代码 | 智能合约 |
|---|---|
| 跑在你家服务器 | 跑在**全网共享的公共服务器**（任何人能看代码、能调函数）|
| 改 DB 是私有动作 | **改状态是公开历史**（不可删除、可被全网索引）|
| 跑一次几乎免费 | **全网每个节点都要算一遍**，所以要付 gas |
| 部署后随时改 | **基本改不动**（除非提前在代码里留了升级机制） |
| owner 权限随便设 | **权限必须显式写在代码里**，且公开可查 |

Handbook 那句最值得记的话：
> **"智能合约把规则变成公共基础设施，也把 bug 变成公共风险。"**

### 2. EOA / 合约账户 / EVM 三者的正确关系

之前把"EVM 是基于智能合约"理解反了。正确关系：**智能合约跑在 EVM 上，不是反过来**。

而 EOA 和 EVM 不是一个层级——EOA 是"账户类型"，EVM 是"执行环境"。正确的对比是 **EOA vs 合约账户**：

```
        ┌─────────────────────────────────────────┐
        │  EVM (Ethereum Virtual Machine)         │  ← 链上 CPU / 执行环境
        │  跑所有交易、检查签名、收 gas、改状态     │
        └─────────────────────────────────────────┘
                      ↑               ↑
              它会处理两种账户的交易：
                      │               │
        ┌─────────────┴───┐   ┌──────┴────────────┐
        │  EOA            │   │  Contract Account │
        │  公钥/私钥       │   │  没私钥           │
        │  钱包持有        │   │  有部署时写死的代码│
        │  能主动发交易    │   │  只能被调用       │
        └──────────────────┘   └────────────────────┘
```

| | **EOA**（如 MetaMask Account 1/2） | **合约账户**（如 Uniswap Router） |
|---|---|---|
| 谁控制 | 持有私钥的人 | 部署时的代码逻辑 |
| 能否主动发交易 | ✅ | ❌ 只能被调用 |
| 有没有代码 | ❌ | ✅ EVM 字节码 |
| 有没有余额 | ✅ | ✅ |
| 地址算法 | 私钥推公钥→哈希后 20 字节 | 部署者地址 + nonce 哈希 |

**关键点**：两者都是"账户"，区别在"谁决定收到调用时怎么响应"——EOA 靠钱包签名拍板，合约靠**部署时就写死的代码**自动响应。

三种典型调用：
1. **EOA → EOA 转账**（0518 做过）：`data` 为空，EVM 不跑代码，只改余额
2. **EOA → 合约调用**：`to` 是合约地址，EVM 加载字节码执行函数
3. **合约 → 合约调用**：在第 2 种交易内部发生，**不需要新签名**，但 gas 还是从最初的 EOA 扣，任意一步 revert 整笔回滚

这就是 **"可组合性"** 的物理基础——合约可以拿其他合约当乐高拼，全自动、原子性。EOA 做不到（没法在一笔交易内多步操作）。

### 3. EVM ≠ 区块链 = 链条，EVM 只是其中"执行层"

区块链至少 5 层：

| 层 | 干什么 |
|---|---|
| 网络层 (P2P) | 节点互相同步交易和区块 |
| 共识层 (PoS/PoW) | 决定谁出块、什么顺序进区块 |
| **执行层 (EVM)** | **给一笔交易，执行后状态变什么** |
| 数据层 | 区块/状态/历史数据存哪儿 |
| 账户/状态存储 (Merkle Patricia Trie) | 全网所有账户状态的高效哈希结构 |

EVM 只管执行层。共识、出块、链怎么连——它都不管。

**反例佐证**：
- Bitcoin 是区块链，但**没有 EVM**（用 Bitcoin Script，几乎不能跑应用）
- Solana 是区块链，但用 **SVM**（Solana VM）不用 EVM
- Arbitrum / Base / Optimism 是**EVM 兼容链**——共识层是 rollup，执行层都跑 EVM，所以以太坊合约能直接搬过去

所以更准确的话：**"EVM 是以太坊系链共享的'执行核心'"**。"链条"指的是数据结构（区块按哈希指针链起来），跟 EVM 是两件事。

### 4. 合约账户的优势：分两种角色看

直觉问的"合约账户感觉没私钥安全"——其实把两个不同对象比错了。要拆开看：

**角色 A：合约账户 = 链上应用本身（绝大多数情况）**
Uniswap / Aave / ERC20 / NFT / DAO 都是合约账户。**它们不是某个人的钱包，是应用**。
EOA 根本干不了这件事——EOA 只能签名转账，没法跑撮合/借贷/投票逻辑。所以这条线上**没有"EOA vs 合约谁安全"的比较**，合约的"优势"等于"它的存在让链上应用层可能"。

**角色 B：合约账户 = 智能钱包（账户抽象 / ERC-4337）**
这才是直觉真正问的。答案：**EOA 适合简单场景，合约钱包适合复杂场景**。

| | EOA | 合约钱包（Safe / ERC-4337） |
|---|---|---|
| 安全模型 | **all-or-nothing**：私钥在=钱在，丢=全丢 | **可编程**：规则写代码里 |
| 私钥泄漏 | 资产全失 | 多签可救（攻击者拿 1/3 不够）|
| 助记词丢 | 永久失去账户 | **社交恢复**：3 个朋友确认换控制权 |
| 防大额误操作 | 没有 | 合约可写"日限额 100 USDC，超额延迟 24h" |
| 给别人/AI 临时授权 | 没有（只能整把私钥给）| **session key**：限时间/金额/白名单 |
| 紧急暂停 | 没有 | 合约可设 emergency pause |
| Gas 怎么付 | 必须 ETH | 可让别人代付（gasless），可用 USDC 付 |
| **谁更安全** | **简单/冷钱包/低金额** | **团队金库 / DAO / AI Agent 代管 / 大额长期持有** |

→ **核心洞察**：EOA 是 2015 年硬编码的账户类型，规则只有一条——"谁有私钥谁说了算"。**账户抽象（Account Abstraction）就是把"账户的规则本身变成可编程的"**。

**对产品研究方向特别重要**：
> **AI Agent 代管资产，几乎只能用合约账户**。把 EOA 私钥交给 Agent = Agent 跑路血本无归；把合约账户的 session key 交给 Agent = Agent 最多在你定的边界里乱来，超出边界合约自己 revert。

这直通长期方向 [bridge/agent-wallet](https://aiweb3.school/zh/handbook/bridge/agent-wallet/)。MetaMask 是 EOA，所以现在的实操经验在 Day 6 学到 agent-wallet 时需要做一个迁移——换成合约钱包（如 Safe）。

## 今日待答 → 已解决（晚上 Q&A 走通）

- [x] 智能合约到底是什么？跟我做过的 ETH 转账什么关系？
- [x] EOA / EVM / 合约账户三者关系？（之前理解反了）
- [x] EVM 是不是 = 区块链？是不是 = 链条？
- [x] 合约账户有啥优势？是不是没私钥安全？

## 留待后续（不强求今天解决）

- [ ] 合约账户"没有私钥"怎么持有 ETH？receive / fallback 函数怎么工作？
- [ ] Layer 2（Arbitrum / Base / OP）和 EVM 是什么关系？rollup 到底是什么？
- [ ] 怎么在 Sepolia 上自己读一个真实合约的 ABI、看它的函数和事件？
- [ ] 账户抽象 ERC-4337 的 UserOperation / Bundler / Paymaster 这套是怎么工作的？
- [ ] 签名分类（personal_sign / EIP-712 / Permit）——留到 Day 5/6 走到 security 章节

## 今天的总结

主题从"签名分类深挖"调整成"智能合约 / EOA / EVM 三层模型"。修正了一个核心认知错误——之前以为"EVM 基于智能合约"，实际是反的：智能合约跑在 EVM 上，EOA 和合约账户都是 EVM 处理的"账户类型"。

最有产品价值的一段是合约账户的两种角色：**作为应用**它让 Web3 应用层成为可能；**作为智能钱包**（账户抽象）它解决了 EOA "all-or-nothing" 的脆弱性。这两个角色之前在脑子里是混的，今晚拆开后，再看 AI Agent 代管资产这个长期问题，逻辑突然清晰——**没有合约账户的可编程边界，AI Agent 拿到 EOA 私钥就等于全权代理，任何安全设计都是事后补救**。

实操层面没新进展，纯概念校准。但这次校准把 Day 1-2 的钱包实操经验和未来 bridge/agent-wallet 的方向第一次串到同一张模型图里，是一次"地基补强"型的学习。

## 下一步（Day 4 计划草案）

- 主题候选：
  - **(a)** dev-stack 一遍过：让"我用过的 MetaMask / Blockscout / Sepolia / RPC"在术语层归位
  - **(b)** 在 Sepolia 上手读一个真实合约（如某个 ERC20）的 ABI 和事件，把今天的概念落地
  - **(c)** 继续 Handbook 顺序读 [cryptography](https://aiweb3.school/zh/handbook/web3/cryptography/)
- 暂定 (b)：实操"读合约"是把今天的认知锚定的最直接方式，对产品研究方向也最实用

## Proof of Work
- [x] 完成 4 轮 Q&A，修正"EVM 基于合约"的认知错误
- [x] 沉淀进本笔记 4 段关键收获 + 1 段总结
- [x] 接入 WCB Agent API，拉取真实 Week 1 任务列表（37 个）+ 重写 learning-plan.md
- [x] 创建 3 个 task 提交版（#9 Learning Agent / #11 概念卡片 / #14 EOA-智能账户-多签 比较）
- [ ] commit + push 本笔记 + 4 个新/改文件
- [ ] 提交 #6 / #9 / #11 / #12 / #14 到 WCB（80-90 分）

## WCB 任务命中清单（晚上接入 API 后才看清）

| WCB Task | 分 | 命中度 | 提交文件 |
|---|---|---|---|
| #6 创建课程 GitHub repo | 10 | 100% | repo 本身（README + 完整结构） |
| #9 完成 Learning Agent Setup | 20 | 100% | `tasks/week1-learning-agent-setup.md` |
| #11 整理 Web3 基础概念卡片 | 10 | 100% | `tasks/week1-web3-concepts-cards.md` |
| #12 完成一笔测试网交易 | 20 | 100% | `tasks/web3-minimum-practice.md`（0518 已存在） |
| #14 比较 EOA、智能账户、多签 | 30 | 100% | `tasks/week1-account-permissions-comparison.md` |
| **合计** | **90** | | |

## 接下来 5 天的滚动节奏（基于 WCB 真实任务）

| 日期 | Cohort Day | 主要任务 | 重点产出 |
|---|---|---|---|
| 05-20 | Day 3 | 5.20 Web3 运行原理 live + 部署一个最小合约 | task #13 (30 分) |
| 05-21 | Day 4 | 5.21 AI 下乡 live + AI 概念卡片 + 信息流清单 | task #8 (10 分) + #37 (20 分) |
| 05-22 | Day 5 | Z.AI + Week 1 例会 live + AI × Web3 流程图 | task #15 (30 分) |
| 05-23 | Day 6 | Open Agentic Economy + Week 1 PoW Pack | task #16 (40 分) |
| 05-24 | Day 7 | 复盘 + 回放任何错过的 live | — |

详细计划见 [`../learning-plan.md`](../learning-plan.md)。

---
*Generated by AI x Web3 School Learning Agent · 对话沉淀型 daily note · WCB API-aligned.*
